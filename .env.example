# MongoDB Atlas — dedicated TaskPro database (taskpro_vg)
MONGODB_URI=
MONGODB_DB=

PORT=

# Optional SMTP — required to email employee credentials for real
# Without these, credentials are logged to the server console + shown to admin in UI
SMTP_HOST=
SMTP_PORT=
SMTP_SECURE=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
